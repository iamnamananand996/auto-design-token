import { Meta } from '@storybook/blocks';

<Meta title="Documentation/Introduction" />

<div style={{ 
  background: 'linear-gradient(to right, #000000, #0A8DD0)',
  padding: '40px 20px',
  marginBottom: '32px',
  borderRadius: '8px',
  color: 'white',
  textAlign: 'center'
}}>
  <h1 style={{ margin: '0', fontSize: '40px', fontWeight: 'bold' }}>Auto Design Tokens</h1>
  <p style={{ fontSize: '20px', marginTop: '16px' }}>A comprehensive design token system with CSS and JavaScript APIs</p>
</div>

## Installation

```bash
npm install auto-design-tokens
# or
yarn add auto-design-tokens
```

## Usage Options

### CSS Import (Recommended)

For React/JavaScript applications:

```jsx
// Import all tokens and light theme (default)
import "auto-design-tokens/dist/index.css";

// For dark theme support (optional)
import "auto-design-tokens/dist/theme/dark.css";
```

For HTML applications:

```html
<!-- Import all tokens and light theme (default) -->
<link rel="stylesheet" href="node_modules/auto-design-tokens/dist/index.css">

<!-- For dark theme support -->
<link rel="stylesheet" href="node_modules/auto-design-tokens/dist/theme/dark.css">
```

Then use the utility classes in your components:

```jsx
export const Demo = () => {
  return (
    <div className="auto-bg-primary auto-text-accent auto-p-4 auto-rounded-lg">
      <h1 className="auto-heading-1">Example Heading</h1>
      <p className="auto-body-default auto-text-secondary">Example text</p>
    </div>
  );
};
```

### JavaScript Import

```jsx
// Import all tokens
import designTokens from 'auto-design-tokens';

```

## Brand Colors


## Core Principles

- **Consistency**: Unified design language across all interfaces
- **Flexibility**: Support for both CSS classes and JavaScript objects
- **Theme Support**: Light and dark mode out of the box
- **Developer Experience**: Easy to use utility classes

## Documentation Structure

- **Documentation** - This section contains general information about the design token system
- **Design Tokens** - Browse available design tokens and CSS utility classes
- **Examples** - Real-world component examples using the design tokens

## Getting Started

1. Install the package: `npm install auto-design-tokens`
2. Import the CSS: `import "auto-design-tokens/dist/index.css"`
3. Start using the utility classes in your components 